{{- if .Values.djangoServer.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "the-monolith.fullname" . }}
  labels:
    {{- include "the-monolith.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "the-monolith.fullname" . }}
  minReplicas: {{ .Values.djangoServer.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.djangoServer.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.djangoServer.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.djangoServer.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.djangoServer.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.djangoServer.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
