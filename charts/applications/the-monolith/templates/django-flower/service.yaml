
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.djangoFlower.name }}
  labels:
    {{- include "the-monolith.labels" . | nindent 4 }}
    app.kubernetes.io/component: {{ .Values.djangoFlower.name }}
spec:
  type: {{ .Values.djangoFlower.service.type }}
  ports:
    - port: {{ .Values.djangoFlower.service.port }}
      targetPort: {{ .Values.djangoFlower.service.port }}
      protocol: TCP
      name: django-server
  selector:
    {{- include "the-monolith.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: {{ .Values.djangoFlower.name }}
