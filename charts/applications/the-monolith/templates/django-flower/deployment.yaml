{{- if .Values.djangoFlower.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.djangoFlower.name }}
  labels:
    {{- include "the-monolith.labels" . | nindent 4 }}
    app.kubernetes.io/component: {{ .Values.djangoFlower.name }}
spec:
  {{- if not .Values.djangoFlower.autoscaling.enabled }}
  replicas: {{ .Values.djangoFlower.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "the-monolith.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: {{ .Values.djangoFlower.name }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "the-monolith.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: {{ .Values.djangoFlower.name }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "the-monolith.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.djangoFlower.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}-worker
          securityContext:
            {{- toYaml .Values.djangoFlower.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: flower
              containerPort: {{ .Values.djangoFlower.service.port }}
              protocol: TCP

          {{- if .Values.djangoFlower.livenessProbe.enabled  }}
          livenessProbe:
            httpGet:
              path: /
              port: flower
            periodSeconds: 10

          {{- end}}

          {{- if .Values.djangoFlower.readinessProbe.enabled  }}
          readinessProbe:
            httpGet:
              path: /
              port: flower
          {{- end}}

          {{- with .Values.djangoFlower.command }}
          command:
            {{- toYaml . | nindent 12 }}
          {{- end }}


          {{- with .Values.djangoFlower.args }}
          args:
            {{- toYaml . | nindent 12 }}
          {{- end }}

          resources:
            {{- toYaml .Values.djangoFlower.resources | nindent 12 }}


          env:
          {{- range $secretMount := .Values.secrets }}
          {{- range $secretMountSubValues := $secretMount.subValues }}
            - name: {{ $secretMountSubValues.envKeyPath | quote }}
              value:  {{ $secretMountSubValues.mountPath | quote}}
          {{- end }}
          {{- end }}
          {{- range $envVar := .Values.env }}
            - name: {{ $envVar.name | quote}}
              value:  {{ $envVar.value | quote}}
          {{- end }}

          volumeMounts:
          {{- range $secretMount := .Values.secrets }}
          {{- range $secretMountSubValues := $secretMount.subValues }}
            - name: {{ $secretMount.name }}
              mountPath:  {{ $secretMountSubValues.mountPath }}
              subPath: {{ $secretMountSubValues.subPath }}
          {{- end }}
          {{- end }}

      volumes:
        {{- range $secretVolume := .Values.secrets }}
        - name: {{ $secretVolume.name }}
          secret:
            secretName:  {{ $secretVolume.secretName }}
        {{- end }}

      {{- with .Values.djangoFlower.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.djangoFlower.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.djangoFlower.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}

{{- end }}
