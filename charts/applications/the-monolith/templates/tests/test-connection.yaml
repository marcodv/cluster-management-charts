apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "the-monolith.fullname" . }}-test-connection"
  labels:
    {{- include "the-monolith.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "the-monolith.fullname" . }}:{{ .Values.djangoServer.service.port }}']
  restartPolicy: Never
